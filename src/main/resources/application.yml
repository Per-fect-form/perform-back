spring:
  application:
    name: perform-back
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/perform"
    username: "root"
    password: "0000"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true